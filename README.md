# bits_and_pieces
POC of Micro Frontends Architecture using Module Federation

# Run Applications

`npm install`
`npm start`

# Run Checkout App
`cd mf-checkout/team-ux`
`npm install`
`npm start`

# Run Product App
`npm install`
`npm start`